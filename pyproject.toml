[tool.poetry]
name = "calibre-annas-archive-tests"
version = "0.0.0"
description = "Tests for calibre-annas-archive parsing logic"
authors = ["Icy Avocado <avocado@ovacoda.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
lxml = "*"
pytest = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
